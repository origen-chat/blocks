import { css } from 'styled-components';

export const transitions = css`
  --xs-transition: 0.05s;
  --sm-transition: 0.1s;
  --md-transition: 0.2s;
  --lg-transition: 0.3s;
  --xl-transition: 0.5s;
`;

export default transitions;
