import { css } from 'styled-components';

export const shadows = css`
  --xs-shadow: 0 0.1rem 0.05rem 0.05rem var(--shadow-color);
  --sm-shadow: 0 0.25rem 0.2rem 0.1rem var(--shadow-color);
  --md-shadow: 0 0.1rem 0.3rem 0.1rem var(--shadow-color);
  --lg-shadow: 0 0.1rem 0.25rem 0.1rem var(--shadow-color);
  --xl-shadow: 0 0.1rem 0.25rem 0.1rem var(--shadow-color);
`;

export default shadows;
